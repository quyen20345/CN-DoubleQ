# ===================================
# LLM Configuration - Tối ưu cho QA
# ===================================
LLM_TYPE=ollama
# Khuyến nghị sử dụng model lớn hơn nếu có thể (trong giới hạn 4B)
CHAT_MODEL=qwen2.5:3b
# CHAT_MODEL=gemma2:2b

# Các model khác < 4B bạn có thể dùng:
# - qwen2.5:3b (KHUYẾN NGHỊ - rất tốt cho tiếng Việt và reasoning)
# - phi3:3.8b (tốt cho instruction following)
# - gemma2:2b (nhanh, hiệu quả)
# - stablelm2:1.6b (nhẹ nhất)

# ===================================
# Embedding Model - Tối ưu cho tiếng Việt
# ===================================
DENSE_MODEL=intfloat/multilingual-e5-base
# DENSE_MODEL=keepitreal/vietnamese-sbert

# Các model embedding tốt cho tiếng Việt:
# - intfloat/multilingual-e5-base (KHUYẾN NGHỊ - tốt cho đa ngôn ngữ)
# - keepitreal/vietnamese-sbert (tối ưu cho tiếng Việt)
# - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# ===================================
# Vector Database (Qdrant) Configuration
# ===================================
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_TIMEOUT=300

# ===================================
# Chunking Settings - Tối ưu cho tài liệu kỹ thuật
# ===================================
# Chunk size lớn hơn để giữ nguyên context của bảng
CHUNK_SIZE=600
CHUNK_OVERLAP=150

# ===================================
# Search Settings - Tối ưu cho độ chính xác
# ===================================
# Số lượng chunks trả về (tăng lên để có nhiều context hơn)
TOP_K=8

# Ngưỡng similarity (giảm xuống để lấy nhiều kết quả tiềm năng hơn)
SIMILARITY_THRESHOLD=0.25

# HNSW search parameter (ef) - càng cao càng chính xác
HNSW_EF=128

# ===================================
# LLM Settings - Tối ưu cho QA
# ===================================
# Temperature thấp cho câu trả lời nhất quán
TEMPERATURE=0.0

# Max tokens cho context
MAX_CONTEXT_TOKENS=2000

# ===================================
# Advanced RAG Settings
# ===================================
# Sử dụng reranking
USE_RERANKING=true

# Sử dụng multiple queries
USE_MULTI_QUERY=true

# Số lượng query variants
NUM_QUERY_VARIANTS=2